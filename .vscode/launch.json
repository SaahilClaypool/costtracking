{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch firefox",
            "type": "firefox",
            "request": "launch",
            "webRoot": "${workspaceFolder}/react-frontend",
            "url": "http://localhost:3000",
        }
    ]
}